namespace knife::sorting {
    template <typename iterator, typename comparator>
    void quick_sort(iterator& begin, iterator& end, comparator& compare){
        size_t size = end-begin;
	size_t median = size/2;
    }
} // knife::sorting namespace
